import { Container, Typography } from "@mui/material";
import "./App.css";

function App() {
  return (
    <Container>
      <Typography variant="h3">
        Vite + React + Material UI + Material React Table V2
      </Typography>
    </Container>
  );
}

export default App;
